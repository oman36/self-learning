# This loads the submodule main_package.sub_package. It must be referenced with its full name:
import main_package.sub_package.some_nasted_module
main_package.sub_package.some_nasted_module.another_func()
